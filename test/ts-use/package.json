{
  "name": "ts-use",
  "private": true,
  "dependencies": {
    "web-encoding": "file:../../"
  },
  "scripts": {
    "test": "npm rm -rf ../../node_modules && npm install && tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^4.2.3"
  }
}
